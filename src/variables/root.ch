PROCEDURE Variable
    LOCAL cNomeQueVoceEscolher := "Se nós nos julgássemos a nós mesmos, jamais seríamos condenados."
    ? cNomeQueVoceEscolher
    ? testx := 1200
    //STORE x TO y, k, z
    // x := y := z := k := 100 

    ? x
    // ? y
    // ? k
    // ? z

    


RETURN

/*
    Declaração e Atribuição :=
    Atribuição =
*/