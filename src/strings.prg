PROCEDURE Strings
    ?? "String com aspas" , ;
      "simples!"
    ? "String com aspas Duplas"
    ? [String com cochete]
    ? "String com " + "Mais de um paramentro!"
    ? 12; ? "Ola, Pessoal"
    ? "2" + "2"
    ? 2 + 2
    //? "3" + 2
    ? 3 + "2"
    ? 2.35 + 3.56
RETURN



